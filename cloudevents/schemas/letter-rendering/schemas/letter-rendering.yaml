---
asyncapi: '3.0.0'
info:
  title: NHS Notify Letter Rendering Events
  version: 1.x
  description: Events generated by the letter-rendering bounded context
defaultContentType: application/json

channels:
  data-plane:
    title: Data Plane
    description: Events relating to message delivery
    messages:
      letter-request-prepared:
        $ref: '#/components/messages/letter-request-prepared'

components:
  messages:
    letter-request-prepared:
      name: letter-request-prepared
      title: Letter Request Prepared
      summary: |
        A letter request has been prepared for dispatch.

        This indicates that a letter has been requested and assigned to a supplier for printing and posting.
      contentType: application/json
      payload:
        $ref: './events/letter-request-prepared-event.schema.json'
      examples:
        - payload:
            $ref: './examples/letter-request-prepared-event.json'

  schemas:
    letter-request:
        $ref: './domain/letter-request.schema.json'
